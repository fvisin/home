* clone with `git clone git@github.com:fvisin/home.git`

* `cd home`

* move every file to parent directory with: `find . -mindepth 1 -maxdepth 1 -exec mv -t.. -- {} +`

* remove home with: `cd ..; rm -r ~/home`
